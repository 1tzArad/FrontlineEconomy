name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Remove UTF-8 BOM from Java files
        run: |
          find . -name "*.java" -exec sed -i '1s/^\xEF\xBB\xBF//' {} +

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'
          server-id: github
          server-username: GITHUB_ACTOR
          server-password: GITHUB_TOKEN

      - name: Build with Maven
        run: mvn clean package -q

      - name: Extract version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> "$GITHUB_OUTPUT"

      - name: Find built JAR
        id: jar
        run: |
          JAR_FILE=$(find target -name "*.jar" | grep -v "original" | head -n 1)
          echo "JAR=$JAR_FILE" >> "$GITHUB_OUTPUT"

      - name: Publish to GitHub Packages
        run: mvn deploy -DskipTests -q
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "FrontlineEconomy ${{ steps.version.outputs.VERSION }}"
          tag_name: ${{ github.ref_name }}
          generate_release_notes: true
          files: |
            ${{ steps.jar.outputs.JAR }}